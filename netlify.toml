# Netlify: приложение в подпапке my-app
# https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  base = "my-app"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
